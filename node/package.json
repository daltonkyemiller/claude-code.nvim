{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "pty-wrapper",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "node ./logging.js --start & node ./pty.js --cmd claude --cols 80 --rows 24 --debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@homebridge/node-pty-prebuilt-multiarch": "^0.12.0"
  }
}
